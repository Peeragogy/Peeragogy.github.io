;; Please set the path as needed, but don’t check in your changes
;; or they will overwrite the default for everyone else.
((nil . ((org-roam-directory . "~/PeeragogyORG/")
         (org-roam-db-location . "~/PeeragogyORG/org-roam.db"))))
